package de.black.core.helper.map;

import java.util.LinkedList;

public class TiledMapObjectHelper {

	public static LinkedList<MapObjectEntity> getObjectIDByName(String name) {

		// TiledMap map = GameState.getInstance().getMap();
		// LinkedList<MapObjectEntity> result= new
		// LinkedList<MapObjectEntity>();
		//
		// for (int gid = 0; gid < map.getObjectGroupCount(); gid++) {
		// for (int oid = 0; oid < map.getObjectCount(gid); oid++) {
		//
		// if (map.getObjectName(gid, oid).equals(name)) {
		// MapObjectEntity found = new MapObjectEntity(gid, oid,
		// map.getObjectName(gid, oid),
		// map.getObjectType(gid, oid),
		// map.getObjectX(gid, oid),
		// map.getObjectY(gid,oid),
		// map.getObjectWidth(gid, oid),
		// map.getObjectHeight(gid, oid));
		// result.add(found);
		// }
		// }
		// }
		// return result;
		return null;
	}

	public static LinkedList<MapObjectEntity> getObjectIDByType(String type) {

		// TiledMap map = GameState.getInstance().getMap();
		// LinkedList<MapObjectEntity> result= new
		// LinkedList<MapObjectEntity>();
		//
		// for (int gid = 0; gid < map.getObjectGroupCount(); gid++) {
		// for (int oid = 0; oid < map.getObjectCount(gid); oid++) {
		//
		// if (map.getObjectType(gid, oid).equals(type)) {
		// MapObjectEntity found = new MapObjectEntity(gid, oid,
		// map.getObjectName(gid, oid),
		// map.getObjectType(gid, oid),
		// map.getObjectX(gid, oid),
		// map.getObjectY(gid,oid),
		// map.getObjectWidth(gid, oid),
		// map.getObjectHeight(gid, oid)
		// );
		// result.add(found);
		// }
		// }
		// }
		// return result;
		return null;
	}

}
